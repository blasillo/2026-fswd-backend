
spring:
  datasource:
    url: "jdbc:oracle:thin:@localhost:1521:FREE"
    username: "tareas"
    password: "S3cr3t0"
    driver-class-name: "oracle.jdbc.OracleDriver"
  jpa:
    hibernate:
      ddl-auto: "validate"
      format: "true"
    show-sql: true
    database-platform: "org.hibernate.dialect.OracleDialect"
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/tareas-app
  docker:
    compose:
      skip:
        in-tests: false


management:
  endpoints:
    web:
      exposure:
        include: "*"